plugins {
	id "java"
	id "application"
}

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

mainClassName = "Main"

dependencies {
	compileOnly(project(":application"))
	annotationProcessor(project(":application"))
}
