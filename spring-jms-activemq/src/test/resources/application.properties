server.port=51120

management.security.enabled=false
spring.schedule.enable=false

# queue
spring.activemq.broker-url=vm://localhost?brokerConfig=xbean:activemq.xml
spring.activemq.in-memory=true

# database
spring.datasource.url=jdbc:h2:mem:test
